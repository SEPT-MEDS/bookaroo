package meds.bookaroo.purchaseservice.responseDTO;

public class CreatePurchaseResponseDTO extends StdResponseDTO {
  public CreatePurchaseResponseDTO(boolean isSuccess, String error) {
    super(isSuccess, error);
  }
}
